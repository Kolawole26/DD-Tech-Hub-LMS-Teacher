import AttendanceContent from '@/components/teacher/AttendanceContent';

export default function AttendancePage() {
  return <AttendanceContent />;
}

export const metadata = {
  title: 'Attendance Management | EduPlatform',
  description: 'Track and submit student attendance for all class types',
};