import TeacherSettingsContent from '@/components/teacher/TeacherSettingsContent';

export default function SettingsPage() {
  return <TeacherSettingsContent />;
}

export const metadata = {
  title: 'Settings | EduPlatform',
  description: 'Configure your teacher account and preferences',
};